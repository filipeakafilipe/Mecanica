<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:prism="http://prismlibrary.com"
             prism:ViewModelLocator.AutowireViewModel="True"
             x:Class="App.Views.CadastrarVeiculoPage"
             Title="{Binding Title}">

    <StackLayout>
        <ScrollView>
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>

                <Editor Grid.Column="0" Grid.ColumnSpan="1" Grid.Row ="1"
                        Placeholder="Marca"
                        Text="{Binding Marca}"
                        Margin="30,0,30,0"
                        />
                <Editor Grid.Column="0" Grid.ColumnSpan="1" Grid.Row ="2"
                        Placeholder="Nome"
                        Text="{Binding Nome}"
                        Margin="30,0,30,0"
                       />
                <Editor Grid.Column="0" Grid.ColumnSpan="1" Grid.Row ="3"
                        Placeholder="Especificação"
                        Text="{Binding Especificação}"
                        Margin="30,0,30,0"
                       />
                <Editor Grid.Column="0" Grid.ColumnSpan="1" Grid.Row ="4"
                        Placeholder="Ano"
                        Text="{Binding Ano}"
                        Margin="30,0,30,0"
                        />
                <Editor Grid.Column="0" Grid.ColumnSpan="1" Grid.Row ="5"
                        Placeholder="Modelo"
                        Text="{Binding Modelo}"
                        Margin="30,0,30,0"
                        />
                <Editor Grid.Column="0" Grid.ColumnSpan="1" Grid.Row ="6"
                        Placeholder="Kilometragem"
                        Margin="30,0,30,0"
                        Text="{Binding Kilometragem}"/>
                <Editor Grid.Column="0" Grid.ColumnSpan="1" Grid.Row ="7"
                        Placeholder="Placa"
                        Margin="30,0,30,0"
                        Text="{Binding Placa}"/>
                <Button Grid.Column="0" Grid.ColumnSpan="1" Grid.Row="8"
                Text="Cadastrar" BackgroundColor="#2296f3" Padding="20" HorizontalOptions="Center" TextColor="White" Command="{Binding CadastrarCommand}"/>
            </Grid>

        </ScrollView>
    </StackLayout>
  
</ContentPage>